{
  "name": "@solstice/provider-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared provider reliability control plane — timeouts, retries, rate limits, bulkheads, circuit breakers",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test --test-reporter=spec src/**/*.test.ts"
  },
  "engines": {
    "node": ">=22"
  }
}
