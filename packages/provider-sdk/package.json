{
  "name": "@solstice/provider-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Universal governed outbound HTTP transport for SunRey external provider adapters",
  "description": "SunRey universal external-data provider SDK — lifecycle, registry, catalog integration, and adapter contracts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test src/**/*.test.ts"
  },
  "engines": {
    "node": ">=22"
  }
}
