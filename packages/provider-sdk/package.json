{
  "name": "@solstice/provider-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SunRey universal external-data provider SDK — lifecycle, registry, catalog integration, transport, reliability, and adapter contracts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test --test-reporter=spec src/**/*.test.ts"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
