# sunrey-bench baseline

Compact engineering baseline for Chunk 58. Numbers are
`ENGINEERING_MEASUREMENT` values captured on a development host.

Do not publish these as production TPS guarantees.

- `manifest.json` — compact committed baseline used by `sunrey-bench compare`
- Nightly artifacts are written beside this file and are not required in git
