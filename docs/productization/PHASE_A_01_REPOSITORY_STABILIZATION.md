# Phase A Prompt 1 — Repository stabilization

This is a productization hygiene record. It is not production authorization,
legal advice, or a claim that external gates are satisfied.

No production activation occurred.

- CORE_CODE_COMPLETE_CANDIDATE=true
- PRODUCTION_READY=false
- PRODUCTION_ACTIVE=false
- LIVE_CONNECTIVITY_ENABLED=false

GENERAL CORE ARCHITECTURE FEATURE EXPANSION COMPLETE remains the engineering
freeze. This work repaired merge debris only.

## 1. Starting repository state

Inspected on 2026-08-21 before edits:

| Item | Value |
| --- | --- |
| Branch | `main` (clean, tracking `origin/main`) |
| HEAD | `7a2ead2d03461381e117007595f0fee0d1390252` |
| Tip commit | Merge pull request #206 (`cursor/sunrey-engineering-closure-be34`) |
| Working tree | clean |
| Work branch created | `cursor/repository-stabilization-cd1e` |

Recent history included overlapping merges for Chunks 164–168 (launch freeze,
ceremony, staged activation, abort/recovery, engineering closure) plus later
CI and persistence repairs. Those merges left silent JSON last-key-wins
damage.

`docs/productization/` did not exist. It was created for this record.

## 2. Issues found

1. Root `package.json` had **two** `"test"` script keys (lines 14 and 15).
   Strict JSON integrity rejected the file. JavaScript last-key-wins would
   have kept only the second command.
2. First `"test"` command included `packages/sunrey-chain/src/native-assets/`
   (Chunk 168). Second included `packages/sunrey-chain/src/production-ceremony/`
   and `packages/sunrey-chain/src/release-candidate/mainnet/` (Chunks 164–167).
   Neither command was a complete superset.
3. `docs/architecture/manifest.json` had two unseparated `"notes"` keys on
   the `sunrey-production-handoff` **component** (Chunk 167 rollback language
   vs Chunk 168 engineering-closure language). The file did not parse.
4. The same capability record later in the manifest had the same unseparated
   `"notes"` collision.
5. `docs/architecture/integrity-baseline.json` had duplicate count keys
   (`canonicalCapabilityCount` 160 then 161, `canonicalComponentCount` 133
   then 134, `chunkDeclarationCount` 162 then 165).
6. `docs/architecture/sunrey-blockchain-protocol.json` `moonreyCoin` had
   leftover stale keys (`currentAuthority: not-implemented`,
   `implemented: false`) stacked on the implemented native-asset values.
7. Engineering closure hashes were stale (`sourceCommit` still
   `aa3331c9…` after later merges).
8. TypeScript regression in
   `packages/sunrey-chain/src/governance-ops/launch-abort/compose-ceremony.ts`:
   compared `privateKeysReused` (`false | undefined`) to `true`.
9. Secret scan hit a PEM-header literal in
   `packages/sunrey-chain/src/release-candidate/mainnet/launch-freeze.test.ts`.
   This was a rejection fixture, not a real key.

Not found:

- Git conflict markers (`<<<<<<<` / `=======` / `>>>>>>>`) in tracked source
- `.orig`, `.rej`, `.bak`, `.tmp` merge debris
- Duplicate package script keys other than `"test"`
- Duplicate capability or component IDs after the notes repair
- Real production secrets, live provider defaults, or `LIVE_*` drift
- `ENVIRONMENT` other than `simulation`
- Helm `productionAuthorized` other than `false`

Naive YAML same-indent key scans produced false positives (OpenAPI paths,
multi-document Kubernetes manifests, repeated CI `run:` steps). Those files
were not merge-corrupted.

## 3. Files repaired

| File | Repair |
| --- | --- |
| `package.json` | One canonical `"test"` script: union of both colliding commands |
| `scripts/check-merge-integrity.mjs` | Required-test-family needles for native-assets, production-ceremony, and mainnet freeze |
| `tests/repository-integrity.test.ts` | Clean fixture covers the new needles |
| `docs/architecture/manifest.json` | Merged overlapping `sunrey-production-handoff` notes (component + capability) |
| `docs/architecture/integrity-baseline.json` | Counts rewritten from live source |
| `docs/architecture/sunrey-blockchain-protocol.json` | Removed stale MoonRey keys; kept implemented native-asset values |
| `docs/architecture/SUNREY_ENGINEERING_CLOSURE.md` | Regenerated by existing write command |
| `docs/architecture/sunrey-engineering-closure.json` | Regenerated by existing write command |
| `packages/sunrey-chain/src/governance-ops/launch-abort/compose-ceremony.ts` | Type-correct `privateKeysReused` read |
| `packages/sunrey-chain/src/release-candidate/mainnet/launch-freeze.test.ts` | PEM header constructed at runtime (`'-----BEGIN ' + 'PRIVATE KEY-----'`) |

`package-lock.json` was not changed. `npm ci --ignore-scripts` succeeded.

## 4. Duplicate / configuration problems resolved

Canonical root `"test"` command now includes all of:

- previous shared package globs
- `packages/sunrey-chain/src/native-assets/*.test.ts`
- `packages/sunrey-chain/src/production-ceremony/*.test.ts`
- `packages/sunrey-chain/src/release-candidate/mainnet/*.test.ts`
- existing nested regulated, payments, persistence, and economic-RC suites
- `tests/*.test.ts` (includes Chunk 167 abort/recovery)

`REQUIRED_TEST_FAMILIES` now fails CI if native-asset, ceremony, or mainnet
freeze coverage is dropped again.

Manifest notes preserve both:

- Chunk 167 application-rollback / `APPLICATION_ROLLBACK_IS_NOT_CHAIN_HISTORY_ROLLBACK`
- Chunk 168 engineering-closure / `CORE_CODE_COMPLETE_CANDIDATE` is an engineering label only

MoonRey protocol object keeps `implemented: true` and
`productionIssuanceImplemented: false`. Tickers remain `NOT_ASSIGNED`.

## 5. Generated files regenerated

Integrity baseline counts were derived from parsed `manifest.json` plus
`docs/architecture/chunks/*.json`, not invented:

| Field | Live count |
| --- | --- |
| `architectureSchemaVersion` | 1 |
| `canonicalPackageCount` | 49 |
| `canonicalCapabilityCount` | 161 |
| `canonicalComponentCount` | 134 |
| `chunkDeclarationCount` | 166 |
| `singleTestScriptInvariant` | true |

Engineering closure documents were rewritten by the existing owner:

```
SUNREY_FIXTURE_ENV=local node --experimental-strip-types --disable-warning=ExperimentalWarning packages/sunrey-chain/src/ops/cli-dispatch.ts production engineering-closure write
```

Result (`ok: true`):

- sourceCommit: `7acfd5453de0a41bd19f3d043fe8ba65f1708224`
- architectureManifestHash: `5b759fa4d44c2303b9054ccee4abe89a535a2c7d09e6f1a24527f2d79c2542ac`
- launchCandidateFreezeHash: `a747fc79123ac5bfa7ce7e232d57d47dfa78487def33c2f2eb7edccf4da9314a`
- closureHash: `78dce7b7055ce4f94b2021e5f8a625461f2a52a84840968f4538d3b1b674ae8d`
- CORE_CODE_COMPLETE_CANDIDATE=true
- PRODUCTION_READY=false
- PRODUCTION_ACTIVE=false
- LIVE_CONNECTIVITY_ENABLED=false
- external inputs remain `present=false`
- human decisions remain `unresolved=true`, `aiMayDecide=false`

The write command does not add a generated-file banner. Existing convention
is to overwrite those two closure files in place.

## 6. TypeScript validation result

Command: `npm run typecheck` (`tsc --noEmit -p tsconfig.json`)

- First run: failed (`TS2367` in `compose-ceremony.ts`)
- After repair: **pass**

No `tsconfig` weakening and no new `any` suppressions.

## 7. Rust validation result

`packages/sunrey-chain/rust` (CI workspace):

```
cd packages/sunrey-chain/rust
cargo fmt --check
cargo check --workspace --locked
cargo clippy --all-targets --locked -- -D warnings
cargo test --workspace --locked
```

All four commands **passed**. `cargo test --workspace --locked -- --list`
reported **194** tests. The executed run finished with no failures.

`packages/sunrey-chain/node` (CI `test:sunrey-node` workspace):

```
cd packages/sunrey-chain/node
cargo fmt --check
cargo check --locked
cargo clippy --all-targets --locked -- -D warnings
cargo test --locked
```

All four commands **passed**. `-- --list` reported **53** tests. The executed
run finished with no failures (34 lib + 6 abuse + 8 accountability +
2 consensus_network + 1 devnet_scenario + 1 exchange_settlement +
1 native_assets).

Clippy remained `-D warnings`. Consensus and native-asset economics were not
changed.

## 8. npm test result

Command: `npm test` from repository root.

After the integrity-fixture update:

- tests: **3073**
- pass: **3072**
- fail: **0**
- skipped: **1** (pre-existing skip; not removed)
- suites: 430

The canonical command reached native-asset, production-ceremony,
mainnet-freeze, abort/recovery, Kernel gating, ledger, deployment-posture,
chain, Exchange, compliance, provider-simulation, and persistence unit
suites included in the root glob.

`npm test:persistence` was **not** run. That remains the separate
PostgreSQL CI job.

## 9. Secret scan result

Commands:

```
python3 scripts/secret-scan.py
python3 scripts/secret-scan.py --self-test
```

First tree scan failed on the launch-freeze PEM fixture. After constructing
the header at runtime (same pattern as `audit.test.ts` /
`audit.remediation.test.ts`):

- Secret scan: **ok**
- Secret scan self-test: **ok**

No real secrets were present. The fixture is not a private key.

`.env` example (`infra/postgres/.env.example`) uses
`solstice_dev_only_*` simulation passwords only.

Helm production-candidate values remain
`productionAuthorized: false`, `mainnetEnabled: false`.

`packages/config/src/flags.ts` remains `ENVIRONMENT = 'simulation'` and every
`LIVE_*` flag `false`.

## 10. Architectural invariants result

Commands and results:

| Command | Result |
| --- | --- |
| `node scripts/check-json-integrity.mjs` | json integrity: ok |
| `node scripts/check-merge-integrity.mjs` | merge integrity: ok |
| `npm run integrity:check` | ok |
| `npm run integrity:report` | JSON_INTEGRITY=true, MERGE_MARKERS_PRESENT=false, PACKAGE_TEST_KEY_COUNT=1, capability/component/chunk IDs unique, CANONICAL_OWNER_COLLISIONS=0, LIVE_FLAGS_CHANGED=false |
| `python3 scripts/lint-architectural-invariants.py` | Architectural invariants: ok |
| `python3 scripts/extraction-dryrun.py` | ok (40 package(s)) |
| `python3 scripts/check-deployment-posture.py` | ok (simulation-only, live flags off) |
| `node scripts/check-kernel-gating.mjs` | Kernel gating CI passed (72 registered paths, all Kernel-authorized) |
| `npm run lint:architecture` | architectural-linter: ok |
| `npm run naming:audit` | ok (protocol_ids_unchanged=true) |

## 11a. Post-merge collision from `main` (#208)

After the first two commits, GitHub merged `main` into this branch
(`a7dcdcec`). `main` had landed `#208` (`c2113ff0`), which independently
repaired the same mashed keys. The merge restacked:

- two `"notes"` keys on the `sunrey-production-handoff` capability
- two `const material` declarations in the launch-freeze PEM fixture
- two engineering-closure hash lists / JSON documents

Repair:

- kept the **superset** capability note (includes
  `Binds existing component evidence by hash` and
  `PRODUCTION_CANDIDATE_REVIEW_READY is not production authorization`)
- kept `#208`'s simulation PEM marker
  (`-----BEGIN SIMULATION PRIVATE KEY-----fixture`) so the secret scanner
  stays quiet while private-key rejection still fails closed
- regenerated closure docs with `engineering-closure write`

Integrity preflight, typecheck, secret scan, repository-integrity tests,
and launch-freeze tests were re-run after this repair.

## 11. Remaining known issues

These are **not** claimed as repaired:

1. Persistence integration tests (`npm run test:persistence`) were not run
   here. They require empty PostgreSQL and stay in the separate CI job.
2. The full GitHub Actions CI job also runs demos, fuzz/formal smoke, SBOM,
   and release-audit steps. Those were not re-executed locally.
3. `packages/sunrey-chain/package.json` still has a **package-local** `"test"`
   script that is a subset of the root canonical suite. Running `npm test`
   from that package directory (wrong cwd) can throw
   `floating container image rejected: unavailable` during freeze binding.
   The **root** `npm test` is the intended complete suite and passed.
4. External / human production gates remain unresolved: licensing,
   regulatory approvals, counsel opinions, external security audit,
   penetration test, bank/BaaS, payment rails, FX, KYC/AML, Travel Rule,
   qualified custody, oracle licenses, production HSM/KMS, validator
   operators, production infrastructure, DNS/certificates/cloud
   credentials, staffing, on-call, human governance signatures, and final
   economic parameters. The closure register still lists them `present=false`.
5. GitHub repository name remains `reyesnick54/solstice` (manual-review
   legacy identity). Not a merge defect.
6. One pre-existing skipped unit test remains skipped.

## 12. Production activation statement

No production activation occurred.

- `ENVIRONMENT` was not changed from `simulation`.
- No `LIVE_*` flag was turned on.
- `productionAuthorized` / `mainnetEnabled` defaults remain false.
- The Kernel, Execution Authority, ledger, Evidence Vault, and account-class
  owners were not replaced.
- AI agent boundaries were not widened.
- No Ethereum / EVM / ERC-20 assumptions were introduced.
- Money remains bigint minor units.

It is safe to proceed to Phase A Prompt 2 (productization, not architecture
invention) only after this record and the checks above. Prompt 2 was not
started.
