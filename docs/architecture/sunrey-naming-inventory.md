# SunRey naming inventory

Generated by `scripts/sunrey-naming-audit.mjs` from
[`sunrey-naming-constitution.md`](./sunrey-naming-constitution.md).
Machine copy: [`sunrey-naming-inventory.json`](./sunrey-naming-inventory.json).

Current master brand: **SunRey**. Legacy master brand is not active.
GitHub repository path remains `reyesnick54/solstice`.

## MUST_MIGRATE

- `root-package-name` — `solstice` → `sunrey` (`package.json`)
- `root-package-description` — `Solstice digital banking simulation` → `SunRey digital banking and dual-economy simulation` (`package.json`)
- `agents-header` — `Solstice agent rules` → `SunRey agent rules` (`AGENTS.md`)
- `readme-heading` — `# solstice` → `# SunRey` (`README.md`)
- `constitution-title` — `Solstice canonical architecture constitution` → `SunRey canonical architecture constitution` (`docs/architecture/constitution.md`)
- `persistence-log-prefix` — `solstice.persistence` → `sunrey.persistence` (`packages/persistence/src/logging.ts`)
- `sdk-display-metadata` — `Solstice SDK` → `SunRey SDK / SunRey Chain / SunRey Exchange` (`packages/sunrey-sdk/src/ids.ts`)
- `explorer-display-metadata` — `Solstice Explorer` → `SunRey Explorer` (`packages/sunrey-explorer/src/taxonomy.ts`)

## MIGRATE_WITH_ALIAS

- `persistence-test-env` — `SOLSTICE_PERSISTENCE_TEST` → `SUNREY_PERSISTENCE_TEST` (`packages/config/src/env.ts`)
- `persistence-pg-env` — `SOLSTICE_PG_*` → `SUNREY_PG_*` (`packages/config/src/env.ts`)
- `sunrey-identity-id` — `SolsticeIdentityId` → `SunReyIdentityId` (`packages/identity/src/ids.ts`)
- `as-sunrey-identity-id` — `asSolsticeIdentityId` → `asSunReyIdentityId` (`packages/identity/src/ids.ts`)

## PRESERVE_IMMUTABLE

- `event-schema-refs-v1` — `solstice.<namespace>.<event>/1` → `unchanged stored schemaRef` (`packages/events/src/taxonomy.ts`)
- `hash-domains` — `SUNREY_*` → `SUNREY_*` (`packages/sunrey-chain/src/protocol/constants.ts`)
- `network-chain-ids` — `net_sunrey_simulation / chn_sunrey_simulation` → `unchanged` (`packages/sunrey-explorer/src/taxonomy.ts`)
- `protocol-asset-ids` — `SUNREY_COIN / MOONREY_COIN` → `SUNREY_COIN / MOONREY_COIN` (`packages/sunrey-sdk/src/ids.ts`)
- `github-repository-path` — `reyesnick54/solstice` → `reyesnick54/solstice` (`https://github.com/reyesnick54/solstice`)
- `npm-scope` — `@solstice/*` → `@solstice/*` (`packages/*/package.json`)
- `database-names` — `solstice_customer / solstice_ledger / solstice_evidence / solstice_security` → `unchanged` (`packages/persistence/src/env.ts`)
- `legal-entity-ids` — `le_solstice_* / SOLSTICE_UK` → `unchanged` (`services/accounts/src/catalog.ts`)
- `component-id-solstice-identity` — `solstice-identity` → `solstice-identity` (`docs/architecture/manifest.json`)

## MANUAL_REVIEW

- `legal-entity-display-names` — `Solstice UK Ltd (simulation)` → `not migrated` (`services/accounts/src/catalog.ts`)
- `webauthn-rpid` — `simulation.solstice.local` → `not migrated` (`packages/identity/src/service.ts`)
- `pdv-export-format` — `SolsticePersonalDataExportV1` → `not migrated` (`packages/personal-data-vault/src/taxonomy.ts`)

## HISTORICAL_ONLY

- `historical-adrs` — `Solstice` → `unchanged historical text` (`docs/architecture/adr/`)

Public current-product Solstice display names remaining: **0**.
