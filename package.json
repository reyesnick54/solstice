{
  "name": "solstice",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Solstice digital banking — Compliance Kernel, simulated Global Money Fabric, and Pyramid Exchange simulation",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "demo": "npm run demo -w @solstice/demo",
    "gate": "node scripts/check-kernel-gating.mjs && node scripts/check-phase9-exchange.mjs",
    "lint:invariants": "python3 scripts/lint-architectural-invariants.py",
    "check:extraction": "python3 scripts/extraction-dryrun.py",
    "check:posture": "python3 scripts/check-deployment-posture.py",
    "typecheck": "npm run typecheck -w @solstice/kernel --if-present && npm run typecheck -w @solstice/ledger --if-present && npm run typecheck -w @solstice/payments --if-present && npm run typecheck -w @solstice/pyramid-exchange --if-present && npm run typecheck -w @solstice/demo --if-present",
    "scan:secrets": "python3 scripts/secret-scan.py",
    "ci": "bash scripts/ci.sh"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "typescript": "^5.9.2"
  }
}
