{
  "name": "@solstice/platform-api",
  "version": "0.1.0",
  "description": "SunRey Platform API — canonical application-facing HTTP runtime, gateway, and Consumer BFF",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./consumer": "./src/consumer/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test src/*.test.ts",
    "start": "node --experimental-strip-types --disable-warning=ExperimentalWarning src/main.ts"
  },
  "engines": {
    "node": ">=22"
  }
}
